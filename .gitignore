*Zone.Identifier
__pycache__/
.idea/
.mailmap

# Agent worktrees — transient, never part of the repo
.claude/worktrees/

# Release APKs are attached to GitHub releases, never committed — 13 MB each.
*.apk
